spring:
  application:
    name: storage
  # datasource comes from SPRING_DATASOURCE_URL / _USERNAME / _PASSWORD
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

storage:
  jwt-secret: ${JWT_SECRET}

server:
  port: ${PORT:8081}
